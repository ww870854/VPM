<Window x:Class="VPM.DuplicateFixConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VPM"
        xmlns:Language="clr-namespace:VPM.Language;assembly=VPM"
        Title="{Binding [Confirm_Duplicate_Fix_Operation], Source={x:Static Language:LanguageManager.Instance}, Mode=OneWay}"
        Height="700" Width="1000"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontFamily" Value="Consolas, Courier New"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#252526" Padding="20,15">
            <StackPanel>
                <TextBlock x:Name="TitleText" Text="{Binding [DUPLICATE_FIX_OPERATION_DETAILS], Source={x:Static Language:LanguageManager.Instance}, Mode=OneWay}" 
                          FontSize="18" FontWeight="Bold" Foreground="#4EC9B0"/>
                <TextBlock x:Name="SummaryText" Text="" 
                          FontSize="12" Foreground="#9CDCFE" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Scrollable Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                     HorizontalScrollBarVisibility="Auto"
                     Padding="20">
            <TextBlock x:Name="ContentText" 
                      TextWrapping="NoWrap"
                      FontSize="11"
                      LineHeight="16"/>
        </ScrollViewer>
        
        <!-- Warning -->
        <Border Grid.Row="2" Background="#3E2723" Padding="15,10" Margin="20,0,20,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="–" FontSize="20" Foreground="#FF9800" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding [Confirm_Duplicate_Fix_Operation_WARNING], Source={x:Static Language:LanguageManager.Instance}, Mode=OneWay}" 
                          FontSize="13" FontWeight="Bold" Foreground="#FF9800"/>
            </StackPanel>
        </Border>
        
        <!-- Buttons -->
        <Border Grid.Row="3" Background="#252526" Padding="20,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" Content="{Binding [Confirm_Cancel], Source={x:Static Language:LanguageManager.Instance}, Mode=OneWay}" Width="120" Margin="0,0,10,0" Click="CancelButton_Click"/>
                <Button x:Name="ConfirmButton" Content="{Binding [Continue], Source={x:Static Language:LanguageManager.Instance}, Mode=OneWay}" Width="120" 
                       Background="#0E639C" Click="ConfirmButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

